<?php
//Include database connection
if(isset($_POST['load'])) {
    $id = $_POST['load']; //escape string
    echo $id;
    // Run the Query
    // Fetch Records
    // Echo the data you want to show in modal
 }
?>